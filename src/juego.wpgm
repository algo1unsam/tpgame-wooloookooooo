import wollok.game.*
import personajes.*
import movimientos.*

program NaveGame {
	
//	CONFIG	
	game.title("UFO Rescue")
	game.height(20)
	game.width(20)
	//game.cellSize(50)
	game.boardGround("fondoluna.png")
	
//	VISUALES
	game.addVisual(nave)
	
//	TECLADO
	keyboard.up().onPressDo { nave.moverseA(arriba) }
	keyboard.down().onPressDo { nave.moverseA(abajo) }
	keyboard.left().onPressDo { nave.moverseA(izquierda) }
	keyboard.right().onPressDo { nave.moverseA(derecha) }
	
//	SONIDO
	const music = game.sound("ufoInvasion.mp3")
	music.shouldLoop(true)
	keyboard.shift().onPressDo({music.pause()})
	keyboard.control().onPressDo({music.resume()})
	game.schedule(500, {music.play()})
	

//	START
	game.start()
	

}